server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dreamdb
    username: mustafaoguzdemirel  # Mac kullanıcı adın
    password:   # Mac'te local PostgreSQL için genellikle şifre yok
  jpa:
    hibernate:
      ddl-auto: create-drop  # İlk çalıştırmada create-drop kullan (tabloları yeniden oluşturur)
    show-sql: true
    properties:
      hibernate.format_sql: true

openai:
  api-key: ${OPENAI_API_KEY:}

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 604800000  # 7 days in milliseconds
